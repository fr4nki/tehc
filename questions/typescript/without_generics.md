# Реализуй функцию без дженериков

**Функция должна принимать на входе одинаковый тип: `string` или `number` и его же возвращать. Необходимо добавить типы**

```ts
function add(x, y) {
    // return x + y;
}

add(1, 2); //3
add("1", "2"); //12

add("1", 2); //tserror
add([], 2); //tserror
```
